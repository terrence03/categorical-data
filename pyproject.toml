[project]
name = "categorical-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas",
    "numpy",
    "statsmodels",
    "ipykernel",
    "jupyter",
    "matplotlib",
    "seaborn",
]
